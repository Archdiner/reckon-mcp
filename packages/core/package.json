{
  "name": "@reckon/core",
  "version": "0.5.0",
  "description": "Reckon's client-agnostic comprehension-loop brain: elicit → isolated grade → rescue → cold recall. Model runtime and storage are injected ports (LlmBackend, Storage); zero coupling to any provider.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "sqlite": "^5.1.1",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
